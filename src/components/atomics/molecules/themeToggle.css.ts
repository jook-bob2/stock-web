import { style } from '@vanilla-extract/css';

export const container = style({
  position: 'absolute',
  top: 0,
  right: 14,
  height: '100%',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'flex-end',
});
